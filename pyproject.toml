[project]
name = "document-scanner"
version = "0.1.0"
description = "Simple document scanner using OpenCV."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.6",
    "opencv-python>=4.13.0.92",
]